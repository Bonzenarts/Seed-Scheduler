import { Crop } from '../types';
import { defaultCrops } from './defaultCrops';

// Export the default crops for direct access when needed
export const crops = defaultCrops;

// Also export the array directly for convenience
export default defaultCrops;